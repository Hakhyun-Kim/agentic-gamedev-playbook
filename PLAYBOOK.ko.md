# Agentic GameDev Playbook — AI가 만들고 AI가 검증하는 게임 개발 방법론

> 서로 다른 장르와 프레임워크의 게임 4개에서 반복 검증한 "AI 페어 게임 개발" 방식을 정리했습니다. 새 게임을 시작할 때 이 플레이북을 기본 지침으로 활용하면 됩니다.

## 검증된 프로젝트

| 프로젝트 | 장르 | 스택 | 저장소 |
|----------|------|------|--------|
| **백층 던전** (dungeon100) | 3D 로그라이크 던전 크롤러 | React + Three.js + TypeScript + Vite | [dungeon100](https://github.com/Hakhyun-Kim/dungeon100) |
| **용사 수학 디펜스** (defenehero) | 3D 타워 디펜스 (교육) | Vanilla JS + Three.js + esbuild | [defenehero](https://github.com/Hakhyun-Kim/defenehero) |
| **두 문 러너** (door-runner) | 2D 러너 | Vanilla JS + Canvas | [door-runner](https://github.com/Hakhyun-Kim/door-runner) |
| **기와장터** (giwa-village) | 3D 멀티플레이 소셜 (온체인) | React Three Fiber + viem + Solidity | [giwa-village](https://github.com/Hakhyun-Kim/giwa-village) |

장르와 프레임워크는 모두 달랐지만 아래 제시하는 방법론은 4개 프로젝트 전체에 그대로 적용했습니다. 우연히 성공한 한두 개 사례가 아니라 다음 게임에도 그대로 복사해 쓸 수 있는 공통 틀입니다.

네 번째 프로젝트인 '기와장터'는 이전 프로젝트들과 형태가 조금 달랐습니다. 싱글플레이 런 개념이 없다 보니 밸런스 봇으로 사망 분포를 수집할 수 없었고, 핵심 규칙 일부는 Solidity로 스마트 컨트랙트에서 움직였거든요.

"그럼 밸런스 봇을 못 쓰는 건가?" 고민하다가 나온 결과가 2.5(소스 대조), 3.7(결정점 기대값), 3.8(시계 주입) 기법입니다. 방법론이 특정 프로젝트에 딱 안 맞는다고 곧바로 버리기보다는, 그 자리를 대체할 더 가벼운 검증 방식을 먼저 찾아보는 편이 훨씬 효과적이더라고요.

---

## 0. 한 줄 요약

> **복잡성을 외부 에셋이 아니라 코드 안에 두면, AI가 기획부터 구현·밸런싱·검증·배포까지 전 과정을 직접 돌릴 수 있습니다.**

이미지나 사운드 파일을 외부에서 가져오거나 생성 AI 이미지로 채우는 대신, **그래픽은 절차 생성 지오메트리로, 사운드는 Web Audio 합성 코드**로 직접 만들었습니다. 이렇게 하면 모든 산출물이 AI가 직접 읽고 수정할 수 있는 코드 텍스트로 남습니다. 사람은 재미의 방향만 잡고, 구현과 플레이 검증은 AI가 알아서 진행하게 되는 거죠.

이 검증 루프는 출시 후에도 계속 돌릴 수 있습니다. 동일한 밸런스 봇을 야간 스케줄에 걸어 두면 nightly 회귀를 자동으로 잡고, 매일 아침 트렌드를 스카우트해 다음 작업을 제안해 줍니다 (→ [11. 상시 자동화](#11-상시-자동화--사람이-자는-동안-도는-루프)).

---

## 1. 무엇을 만들지 고르는 기준 — 3-Gate

새 게임 아이디어나 신규 기능을 검토할 때 3가지 관문(3-Gate)을 먼저 통과시킵니다. 아무리 흥미로운 기능이라도 이 세 관문을 통과하지 못하면 일단 보류합니다.

| 관문 | 핵심 질문 | 통과 예시 | 탈락 예시 |
|------|-----------|-----------|-----------|
| **① 절차성** | 외부 에셋 없이 코드(시드 난수, 수학 공식, Canvas, Web Audio)로 만들 수 있는가? | 절차 생성 맵, 합성 효과음 | 손그림 스프라이트 시트가 필수인 연출 |
| **② 재미** | 코어 루프에 의미 있는 *결정(선택)*을 더하는가, 아니면 단순 장식인가? | 시너지 조합과 직업 강화를 고르는 순간 | 화면 구석에 배치된 정적 배경 애니메이션 |
| **③ 검증가능** | 순수 로직으로 분리되어 봇이 수치로 측정할 수 있는가? | 웨이브 생존율, 사망 층 분포 | "감성적인 느낌"처럼 수치화가 불가능한 요소 |

- **백층 던전**은 이 기준에 맞춰 "복잡성이 에셋이 아니라 코드에 있는" 절차 생성 로그라이크를 선택했습니다.
- **용사 수학 디펜스**는 이 기준을 통해 단순 숫자만 올리는 강화 대신 행동 양식을 바꾸는 전설 진화 시스템을 채택했습니다.

### 1.1 ②(재미) 관문은 한 번 통과로 끝나지 않는다 — 다른 시스템이 개입하면 재검사할 것

처음에는 유의미했던 선택지가, 시간이 지나면서 적응형 난이도 보정이나 하한선 시스템 등이 추가되면 어느새 단순 의례적인 클릭(장식)으로 변질되기도 합니다. 따라서 3-Gate의 ②번 관문은 기능을 처음 넣을 때뿐만 아니라, 관련 시스템을 수정할 때마다 "이 선택이 여전히 의미가 있는가?"를 다시 확인해야 합니다.

플레이어가 매 판마다 같은 선택 메뉴를 무지성으로 넘기고 있다면, 그건 이미 결정을 내리는 게 아니라 통과 의례를 치르는 셈입니다. 이럴 때는 어설픈 선택지를 지우고 **결과만 보여주는 블라인드 룰렛 형태**로 바꾸는 편이 훨씬 낫더라고요. 무엇이 나오고 무엇을 비껴갔는지 연출로 보여주기만 하면, 불필요한 결정 피로 없이 뽑기의 긴장감만 깔끔하게 남습니다.

> **실무 사례 (용사 수학 디펜스, 2026-08-03):**
> 원래는 수학 관문마다 난이도 카드 3장 중 하나를 고르게 했습니다. 그런데 이미 난이도 생태계에 정답률 기반 적응형 보정 시스템이 들어있어서, 플레이어의 카드 선택은 불필요한 클릭만 늘리는 짐이 되어 있었죠. 한 판에 열댓 번이나 동일한 메뉴를 넘겨야 했습니다.
> 
> 그래서 난이도 선택 메뉴를 아예 지우고 룰렛 방식(스핀 후 자동으로 하나에 정지)으로 교체했습니다. 선택 단계를 제거하니 조작 피로도는 사라지고 긴장감만 남더군요. 밸런스 봇도 동일한 시드 난수로 룰렛을 돌리도록 구현해 검증 로직의 단일성을 유지했습니다.

---

## 2. 아키텍처 4대 규약 (새 프로젝트 시작 시 복사해 쓸 것)

새 게임 프로젝트를 시작할 때 아래 4가지 규약을 **첫 커밋부터** 준수하면, 이후 밸런싱과 자동화 작업이 매우 수월해집니다.

### 2.1 순수 로직과 렌더링 분리 ★ (가장 중요)

게임 규칙(전투·경제·웨이브)은 **DOM이나 3D 렌더러를 전혀 몰라도 작동하는 순수 함수**로 작성합니다. 렌더러와 사운드는 이 로직이 반환하는 이벤트 배열을 받아 시각/청각 효과로 보여주기만 합니다.

```
data.(js|ts)     — 모든 밸런스 수치 (수치 튜닝은 여기서만 진행)
engine.(js|ts)   — 순수 로직: tick(state, dt) → events[] (봇과 실제 게임이 공유)
render.(js|ts)   — 렌더러: state를 화면에 그리고 events를 이펙트로 변환
sfx / music      — events를 오디오로 변환
main / app       — 위 요소들을 연결하는 메인 컨트롤러
```

이 구조 덕분에 **밸런스 봇을 쉽게 돌릴 수 있습니다.** 렌더링 단계(`render`)를 빼고 순수 엔진(`engine`)만 초고속으로 반복 호출하면 되니까요.

### 2.2 시드 결정론 (Seeded Determinism)

모든 난수 생성은 시드 기반 RNG(`mulberry32` 약 11줄)로 통일합니다. "동일한 시드 = 동일한 게임 전개"가 보장되면, 버그 재현과 밸런스 회귀 검증이 매우 직관적이게 됩니다.

```js
function mulberry32(seed) {
  return function () {
    seed |= 0; seed = (seed + 0x6D2B79F5) | 0;
    let t = Math.imul(seed ^ (seed >>> 15), 1 | seed);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
```

엔진은 `createGame({ rng })` 형태로 RNG 함수를 전달받습니다. 실제 게임은 `Math.random`을 넘기고, 밸런스 봇은 `mulberry32(seed)`를 주입해서 시뮬레이션을 수행합니다.

**절차 생성 맵에 새 지형을 추가할 때 주의할 점 (실무 경험):**

1. **분기 판정 난수를 메인 스트림에서 뽑지 말 것:** "이 맵이 특수 지형인가?"를 일반 `rand()`로 추출하면, 그 뒤의 모든 난수 순서가 한 칸씩 밀려서 기존 맵들의 지형까지 엉뚱하게 바뀌어 버립니다. 특수 분기 난수는 시드 기반 파생 스트림으로 따로 돌려야 합니다.
2. **신규 구조물의 난수 추출은 반드시 기존 순서 뒤로 배치:** 중간에 난수 호출을 끼워 넣으면 1번과 마찬가지로 난수 스트림 밀림이 발생합니다.
3. **공용 배치 로직과의 가정을 유지할 것:** 몬스터 스폰 로직이 "방 내부는 평평한 바닥"을 전제로 한다면, 새 지형에서도 열린 사각형 영역만 방으로 지정해야 배치 로직 변경 없이 안전하게 동작합니다.
4. **길찾기 알고리즘과 지형 연결성 정의 맞추기:** BFS 길찾기가 4방향 기준이라면, 지형 연결성 판정도 4방향 기준으로 검사해야 합니다. 대각선으로만 연결된 통로는 그리드상 길로 보여도 봇이 통과하지 못해 영원히 갇히는 현상이 발생합니다.
5. **맵 면적이 달라지면 적 밀도 보정:** 지형 면적이 1.65배 넓어지면 동일한 수의 적이 흩어져 난이도가 의도치 않게 낮아집니다. 면적당 위협 수치를 맞춰야 전후 밸런스 비교가 의미를 갖습니다.

> **생성기 자동 검사 스크립트 작성 팁:**
> 브라우저 렌더링 없이 순수 지형 생성 함수만 호출해서, 수백 개 맵의 도달 가능성·고립 영역·벽 내부 배치 여부를 1초 만에 검사하는 CLI 스크립트(`npm run floor-check`)를 만들어 두면 좋습니다. 지형 로직을 고칠 때 커밋 전에 오류를 미리 잡을 수 있습니다.

### 2.2.1 온체인 및 확률형 게임에서의 결정론 응용

결정론은 원래 봇 검증용으로 설계했지만, 플레이어 신뢰 구축에도 유용합니다. "확률표를 보여주겠다"는 선언보다 **"동일한 시드와 선택으로 당신이 직접 결과를 재현해보라"**고 검증 기능을 제공하는 것이 훨씬 명확하거든요.

```js
// 온체인 트랜잭션 입력값(선택)과 주차 시드(블록 해시)만으로
// 게임 서버 없이 결과를 재현하여 온체인 이벤트와 대조
const replayed = resolveRun(seed, choices);
assert(replayed.floor === onchainEvent.floor);
```

> **기와장터 사례:**
> 던전 진행 판정을 순수 함수 모듈로 구성해두니 `scripts/verify-run.mjs` 스크립트로 결과 트랜잭션을 바이트 단위로 정확히 재현할 수 있었습니다. 게임 화면에도 "🔒 이 결과 검증하기" 버튼을 붙여 사용자가 시드 기반 결과를 온체인 기록과 직접 대조해 볼 수 있게 했습니다.
> 
> *참고: 실시간 요소(`block.timestamp`)가 개입하는 보스전 등은 아카이브 노드 없이 재현하기 어려워 검증 범위에서 제외하고, 이 제한사항도 문서에 명시했습니다.*

### 2.3 밸런스 단일 파일

모든 밸런스 수치(비용, 체력, 스케일링 공식, 드랍 확률)를 `data` 파일 하나에 모아 둡니다. 튜닝 포인트를 일원화하면, 봇과 실게임이 **동일한 수치 파일**을 읽게 되어 "봇 테스트는 통과했는데 실게임에서는 밸런스가 터지는" 현상을 방지할 수 있습니다.

### 2.4 이벤트 방출 구조

`tick()` 메서드는 내부 상태만 변경하고 **해당 프레임에 일어난 사건 목록(이벤트 배열)**을 반환합니다. 렌더러와 사운드 모듈은 이 이벤트를 수신해서 시각 이펙트와 효과음을 출력합니다.

```js
const events = tick(state, dt);   // [{type:'kill', x, y, gold}, {type:'castleHit', dmg}, ...]
renderer.onEvents(state, events); // 시각 효과 연출
handleEvents(events);             // 오디오 및 UI 알림 처리
```

이런 구조라면 밸런스 봇은 이벤트를 그냥 무시하고 넘어가고, 게임 렌더러는 화려한 연출을 입히게 됩니다. **단일 엔진, 두 소비자의 관계**가 성립하죠.

### 2.5 서로 다른 언어 간 수치 대조 — "어긋나면 테스트가 깨지게" 묶기

밸런스 단일 파일 원칙은 한 단일 런타임 내에서만 온전히 작동합니다. 게임 규칙 일부가 스마트 컨트랙트(Solidity)나 서버(TypeScript/C#)에 분산되어 있다면, 파일 하나로 합치기가 힘들죠.

이럴 때는 소스 코드를 직접 파싱해서 수치가 일치하는지 정규식 대조 검사를 작성하는 방법이 유용합니다. 해당 런타임을 실제로 실행하지 않고도 정적 대조가 가능하거든요.

```js
// 스마트 컨트랙트, 서버, 클라이언트의 확률 경계값을 파싱해서 비교
const table = {
  컨트랙트: doorThresholds('contracts/Guilds.sol', 'function doorRoll'),
  서버:     doorThresholds('server/src/guilds.ts', 'doorOutcome('),
  클라이언트: doorThresholds('client/src/chain/guilds.ts', 'function doorRollLocal'),
};
// 셋 중 하나라도 수치가 다르면 테스트 실패
```

이 방식은 TypeScript 빌드 파이프라인 전체를 불러오기 부담스러울 때, Node 테스트 환경에서 정규식으로 불변식을 대조할 때도 똑같이 유용하게 쓰입니다.

### 2.6 세션 저장 (localStorage) 안심 설계

자동 저장을 구현할 때는 ①저장 타이밍, ②쓰기 시점, ③불러오기 데이터 검증의 3가지를 명확히 분리해야 합니다.

1. **저장 시점:** 전투 진행 중이 아닌, 웨이브가 끝난 **안전한 준비 단계**에서만 스냅샷을 찍습니다. 시뮬레이션 중간 상태는 직렬화할 때 참조가 깨지거나 꼼수 저장이 발생하기 쉽습니다.
2. **비동기 안전 쓰기:** 직렬화는 상태 변경 즉시 수행하되, 실제 저장소 쓰기는 `requestIdleCallback`으로 미룹니다. 단, 탭이 숨겨지거나 닫히는 이벤트(`pagehide`, `visibilitychange`) 시에는 즉시 저장소를 비동기 없이 flush해야 데이터 유실을 막을 수 있습니다.
3. **불러오기 데이터 검증:** 사용자가 저장 파일을 수정했더라도 게임이 크래시되지 않도록 클램핑(Clamp) 처리를 해줍니다. 존재하지 않는 ID나 오버플로 수치는 안전한 범위로 자동 조정하거나 벤치로 대피시킵니다.
4. **양방향 회귀 테스트:** 정상적인 데이터 수신 검사 외에도, 의도적으로 유효하지 않은 수치를 넣은 저장 파일을 읽어와도 크래시 없이 안전한 상태로 복원되는지 검증 테스트를 작성합니다.

### 2.7 절차 생성 콘텐츠의 런타임 사양 신고

절차적으로 생성되는 문장이나 문제(예: 수학 퀴즈)가 살아있는 게임 상태(몬스터 체력, 남은 골드 등)를 참조하면, 몬스터 체력이 수만 단위로 치솟을 때 저학년에게 부적절한 나눗셈 문제가 나가는 식의 버그가 생깁니다.

이럴 때는 생성기가 결과를 뱉을 때 **어떤 조건과 제약사항을 검증해야 하는지 메타데이터(`needs`, `calc`)를 함께 반환**하도록 구현합니다.

```js
return {
  text: `오크 ${n}마리를 놓치면 성이 입는 피해는?`,
  answer: hp * n,
  needs: [hp, n],         // 해당 숫자들이 지문에 실제로 노출되어야 함
  calc: { mul: [hp, n] }, // 연산 종류와 피연산자 전달
};
```

생성기가 출력한 결과물이 해당 학년의 연산 범위(`fitsGrade`)를 벗어나면 자동으로 재추첨을 돌리고, 계속 실패할 경우 가장 안전한 기본 문제 유형으로 폴백하도록 만듭니다.

### 2.8 코드 분할 시 인터페이스 유지

프로젝트가 커져 단일 로직 파일이 2,000줄 넘게 부풀어 오르면 `engine/` 폴더 내 여러 모듈로 분할하되, **호출하는 쪽의 import 경로(`engine.js` 배럴 파일)는 변경하지 않고 유지**하는 것이 좋습니다.

```js
// engine.js — 내부 모듈들을 재수출(Re-export)하는 배럴 역할을 수행
export * from './engine/state.js';
export * from './engine/combat.js';

// 사용하는 곳: import { tick } from './engine.js' (경로 변경 없음)
```

호출부를 건드리지 않고 파일 분깃만 진행하면, 리팩토링 커밋의 diff가 순수 "코드 이동"으로만 구성되어 리뷰와 검증(기존 밸런스 봇 실행 결과 비교)이 매우 깔끔해집니다.

---

## 3. AI 자동 밸런싱 ★ (방법론의 핵심)

사람이 일일이 100판씩 기획 수치를 확인하는 대신, **밸런스 봇이 실제 게임 엔진으로 수백 판을 초고속 시뮬레이션하고 사망 분포 리포트를 반환**하게 합니다.

### 3.1 가상 플레이어 프로필

실력대별 플레이어의 행동 특성을 파라미터로 정의합니다.

```js
const PROFILES = {
  '초보': { acc: 0.45, actionsPerTurn: 1, comboChance: 0.15, sloppy: 0.5 },
  '보통': { acc: 0.70, actionsPerTurn: 3, comboChance: 0.70, sloppy: 0.3 },
  '고수': { acc: 0.90, actionsPerTurn: 6, comboChance: 1.0,  sloppy: 0 },
};
```

`acc`(정답/성공률), `sloppy`(실수율) 같은 파라미터로 **불완전한 사람 플레이어의 특성**을 흉내 내는 것이 핵심입니다. 최적의 정답만 내는 완전무결한 플레이만 시뮬레이션하면 실제 사용자가 느낄 난이도를 도출할 수 없습니다.

### 3.2 사망 분포 리포트

N판의 테스트를 돌려 도달 웨이브/층의 분포(평균, 중앙값, 사분위수)를 확인합니다.

```
[보통] 초보  평균 9.1웨이브 (중앙값 9)    ← 목표: 초보자도 초기 웨이브는 통과하고 적절한 벽을 만남
[보통] 보통  평균 17.8웨이브 (중앙값 18)  ← 목표: 중반 웨이브까지 무난히 몰입
[보통] 고수  평균 24.3웨이브 (중앙값 25)  ← 목표: 숙련자는 확실히 더 깊이 진입
```

실력 수준에 맞춰 도달 수치가 단조 증가하는지, 무한 생존(밸런스 붕괴) 현상이 없는지 확인합니다.

### 3.3 기준선 (Baseline) 및 회귀 검증

조율된 난이도 분포를 `baseline.json`에 기록해 두고, 코드 변경 커밋마다 봇을 재실행하여 **중앙값이 허용 오차범위(예: ±3웨이브)를 벗어나면 빌드를 실패** 처리합니다.

```bash
node scripts/balance-bot.mjs 120 check   # 기준선 이탈 시 exit code 1
```

> **용사 수학 디펜스 사례:**
> 전설 특수 능력을 추가했을 때 이 검증이 "고수 난이도 중앙값 22→29 급등"을 즉시 감지해냈습니다. 스노우볼 억제 로직을 추가해 바로 난이도를 보정했죠. 사람의 수동 테스트로는 잡기 힘든 밸런스 회귀였습니다.

*버그 수정으로 인한 분포 변동 주의:*
게임 내부 로직 버그를 고치면서 봇의 중앙값이 상승한 경우, 이는 되돌려야 할 회귀가 아니라 정상화 과정입니다. 이 경우 신규 수치로 `baseline.json`을 업데이트하고 사유를 주석으로 명시해 둡니다.

### 3.4 측정용 봇 vs 사람형 봇의 분리

기준선을 측정하는 봇의 AI 판단을 함부로 고치면, 난이도 변화가 게임 때문인지 봇 알고리즘 때문인지 구분할 수 없게 됩니다. 따라서 봇 역할을 분리합니다.

- **`hard` (측정용 봇):** 기준선 판정 전용. 일단 설정되면 **AI 행동 로직을 동결**합니다.
- **`human` (사람형 봇):** 실제 사람처럼 행동하는 봇. 화면에 표시되는 HUD 정보(HP, 조합 힌트, 테두리)만 읽고 판단합니다.

사람형 봇에 "체력이 낮으면 후퇴" 같은 소극적 행동을 추가할 때는 반드시 **시한(Timeout)**을 두어야 교착 상태(Stuck)에 빠지지 않습니다.

### 3.5 난이도 곡선의 2대 원칙

**① Threat Ramp — 적 체력을 부풀리지 말고 "플레이어가 받는 피해량"을 후반에 가속할 것**

```js
// 적 HP를 부풀리면 초반 구간까지 딱딱해지므로, 후반에 받는 피해만 가속함
export const castleDmgScale = (w) => 1 + Math.max(0, w - 15) * 0.08;
```

**② Soft Cap — 곱연산 폭발 대신 상한선에 점근되도록 설계할 것**

```js
value += (CAP - value) * 0.15;   // 초기 선택은 효과가 크지만 후반에는 자연스럽게 완만해짐
```

백층 던전은 이동속도와 사거리에, 용사 수학 디펜스는 웨이브 HP 곡선에 이 소프트 캡을 적용했습니다.

### 3.6 CI 실행 환경의 3대 주의사항

1. **프레시 프로필 준비:** 봇은 언제나 세이브 데이터가 없는 빈 상태(초기 튜토리얼/인트로)로 시작하도록 배선합니다.
2. **해상도 축소로 픽셀 연산 감소:** CI 헤드리스 환경에서는 뷰포트 크기를 640x360으로 낮춰 시뮬레이션 속도를 극대화합니다.
3. **의도된 404 및 외부 에러 제외:** 404 노이즈(파비콘 등)나 외부 서버 에러로 CI 테스트가 막히지 않도록 필터링합니다.

### 3.7 기대값 기반 닫힌 식 검사 (Close-Form Expectation)

선택지가 단순한 "Push-Your-Luck"(한 번 더 도전할지, 정산하고 나갈지) 메커닉은 수백 판을 돌릴 필요 없이, 손익분기점 기대값이 0이 되는 지점을 수학적 공식을 써서 단언 검사(Assertion)로 묶어둘 수 있습니다.

### 3.8 시계 주입 (Clock Injection)

특정 시간대나 요일에 맞춰 발동하는 이벤트는 `isEventNow(now = Date.now())`처럼 시간 인자를 주입받도록 구성합니다. 로컬 테스트 및 CI에서 시간을 임의로 변경하여 밀리초 단위로 이벤트 동작을 검증할 수 있습니다.

### 3.9 근접사(Near-Miss) 지표 측정

사망률 0%가 단순히 위협이 없었다는 의미는 아닐 수 있습니다. 살아남은 라운드에서 잃은 체력의 최대/평균 폭(근접사 폭)을 함께 측정하면, "죽지는 않지만 식은땀 나는 긴장감"이 유효한지 파악할 수 있습니다.

### 3.10 봇 순간이동 검증 금지

전투 검증 시 봇을 보스나 출구 앞으로 직접 순간이동시키는 유도 테스트는 피하세요. 이동 과정에서 겪는 회피/전투 기회가 지워져 실제 플레이와 완전히 다른 결과가 발생합니다.

### 3.11 전투 중 무거운 렌더/상태 갱신 미루기

실시간 전투 중 정예 몬스터 처치 시 대량의 UI 상태 갱신이 일어마면 프레임 드랍이 생기고, 이 순간 프레임 판정에 의해 봇이 억울하게 사망할 수 있습니다. 보상 지급 처리는 전투가 잠시 소강되는 포털 통과 등의 안전 지점으로 미루는 편이 깔끔합니다.

---

## 4. 그래픽 "인디 탈출" — 외부 에셋 0개로 그럴듯하게 연출하기

모든 시각 효과는 외부 3D 모델이나 2D 이미지 없이 `render` 단계의 코드로 직접 생성합니다.

| 기법 | 기대 효과 | 핵심 구현법 |
|------|-----------|-------------|
| **블롭 그림자 (Blob Shadow)** | 유닛이 바닥에 입체감 있게 붙어 보임 | 유닛 발밑에 방사형 타원 그라디언트 렌더링 |
| **블룸 (Bloom)** | 빛나는 발광(Emissive) 요소 연출 | 가산 합성 및 임계값 블러 패스 적용 |
| **절차적 텍스처** | 1장의 기본 텍스처 + 팔레트 조합 | 그레이스케일 노이즈 × 색상 팔레트 연산 |
| **파티클 풀 (Particle Pool)** | 타격, 폭발, 화염 이펙트 | 메모리 할당 없는 고정 배열 재사용 |
| **데미지 숫자 풀** | 타격 피드백 제공 | Canvas fillText, 상단 이동 후 페이드아웃 |
| **카메라 셰이크** | 피격 및 타격감 전달 | cumulative shock → 제곱 감쇠 연산 |
| **비네트 & 안개** | 공간 깊이감 제공 | 시야 거리 기반 안개 밀도 조절 |
| **절차적 캐릭터** | 로우폴리 3D / 2D 지오메트리 | 프리미티브 조합 + 투구, 지팡이 등 직업 소품 |

```js
// 카메라 셰이크: 피격/타격 시 누적 후 제곱 감쇠
addShake(v) { this.shake = Math.min(0.8, this.shake + v); }
// 프레임 업데이트:
this.shake = Math.max(0, this.shake - dt * 1.7);
const s2 = this.shake * this.shake;
cam.position.x += (Math.random() - 0.5) * s2 * 2.2;
```

### 4.1 조명, 실시간 그림자 및 톤매핑

1. **실시간 그림자:** 방향광(Directional Light) 1개 + PCFSoft 섀도맵을 사용하되, 섀도 카메라 범위를 유닛이 움직이는 전장 공간에만 타이트하게 맞춥니다.
2. **필름 톤매핑:** ACESFilmic 톤매핑을 적용하여 너무 밝은 파티클이나 발광체가 흰색 덩어리로 뭉개지는 현상을 방지합니다.
3. **그림자 생성 검증:** 픽셀 차분(`readPixels`)으로 그림자 켜짐/꺼짐 시 1% 이상의 픽셀 변화가 발생하는지 자동 검사할 수 있습니다.

### 4.2 화면에 보이지 않는 요소 배제하기

탑다운/쿼터뷰 카메라 각도상 화면 바깥에 위치하는 하늘 돔이나 먼 배경 요소는 아예 렌더링 리스트에서 제외하는 것이 좋습니다.

```js
// 카메라 절두체(Frustum) 바깥의 구름/배경 객체는 생성 생략
const v = obj.position.clone().project(camera);
// |v.x| < 1 && |v.y| < 1 && v.z < 1 판정을 벗어나면 스킵
```

또한, 게임을 구동한 지 4초 후 FPS를 실측해서 45fps 미만인 저사양 기기라면 블룸 이펙트와 파티클 수를 줄이는 가벼움(Lite) 모드로 자동 전환하는 로직을 갖춥니다.

### 4.3 외부 이미지 텍스처 적용 시 색상과 질감 분리

외부 이미지 텍스처를 적용할 때는 사진 본연의 색을 그대로 쓰는 컬러맵(`map`) 방식과, 요철/질감만 빌려오는 둠맵(`bumpMap`) 방식을 구분해야 합니다.

- **색상이 중요한 표면(나무, 흙):** `map` 활용
- **이미 색상이 지정된 고유 요소(기와, 무늬 벽):** `bumpMap`만 씌워 요철감만 살림
- **스타일라이즈드 텍스처:** 납작한 컬러면에 실사 사진을 얹으면 어색해지므로 무분별한 텍스처 덧씌우기는 지양

### 4.4 연사 공격 시 카메라 셰이크 대신 위치 기반 임팩트 활용

자동 조준이나 연타 공격이 발생하는 게임에서 평타 명중 시마다 카메라 셰이크를 발생시키면 화면이 지속적으로 흔들려 멀미를 유발합니다.

이럴 때는 전체 카메라 좌표를 흔드는 대신, 맞은 위치 좌표에 충격 파동 이펙트(`ripple(x, z, power)`)를 터뜨리는 편이 시각 전달력과 안정성 면에서 훨씬 우수합니다.

### 4.5 모바일 기기에서의 시야각 및 장식 최적화

모바일 기기에서는 조작 화면이 협소하므로 배경 장식(바람 잔디, 해변)을 생략하고 화각을 넓혀 전장 면적(터치 영역)을 확보해 주는 설계가 유용합니다. `pointer: coarse` 매치 쿼리로 모바일 기기를 감지하여 초기 부팅 시부터 저사양 옵션을 기본 적용합니다.

---

## 5. 사운드 — 외부 오디오 파일 0개로 구현하는 Web Audio 합성

게임 내 모든 효과음과 BGM을 외부 오디오 파일 없이 Web Audio API 기본 합성 코드로 구현합니다.

### 5.1 사운드 합성 원시 도구 (Primitive Tools)

```js
tone(freq, start, dur, type, vol, glideTo)  // 오실레이터 1개 + 게인 엔벨로프
noise(start, dur, vol, freq, q)             // 화이트 노이즈 + 밴드패스 필터
```

위 두 가지 원시 함수를 조합해 효과음 레시피를 정의합니다.
- **성공:** 상승 아르페지오 (도-미-솔-도)
- **실패:** 부드러운 하강음 (오답 시 플레이어를 불쾌하게 하지 않도록 조율)
- **타격음:** 아주 짧은 밴드패스 필터드 노이즈 + 레이트 리밋 (45~70ms)
- **저체력 연출:** 심장 박동음 + 화면 붉은 맥동 이펙트

### 5.2 합성 BGM 구성을 위한 필수 6요소

단순 피치음 시퀀서가 삑삑거리는 기계음처럼 느껴지는 현상을 피하려면 아래 6가지 요소를 적용해야 합니다.

1. **코드 진행 (Chords):** 마디별 화성을 정의하고 트랙 전체가 코드 구성음 내에서 동작하도록 설계
2. **패드 (Pad):** 두 개의 톱니파를 ±6센트 디튠(Detune)하여 겹치고 로우패스 필터로 부드럽게 감싸는 방식
3. **아르페지오 (Arpeggio):** 코드 구성음을 인덱스 패턴(`[0,1,2,1,0,2...]`)으로 순회
4. **베이스 (Bass):** 근음 위주로 로우패스 처리
5. **드럼 (Drums):** 킥(사인 파형 150→48Hz), 스네어(노이즈), 하이햇(하이패스 노이즈) 구성
6. **리버브 (Reverb):** 피드백 딜레이 + 로우패스 필터를 이용한 가벼운 공간감 형성

### 5.3 음소거(Mute) 기능 설계의 필수 체크리스트

1. **BGM과 SFX의 개별 음소거 지원:** 사용자의 가장 흔한 요구나 BGM 단독 끄기를 고려할 것.
2. **HUD 상의 직관적인 토글 버튼:** 설정 메뉴 깊숙이 넣지 말고 HUD 상단에 아이콘+텍스트 조합으로 배치.
3. **음소거 상태의 영속성:** `localStorage`에 설정값을 캐싱.
4. **단축키(M) 지원:** 즉각적인 조작 편의성 제공.
5. **AudioContext 노드 차단 방식:** 오디오 컨텍스트를 닫지 않고 `if (muted) return;` 방식을 채택하여 복원 시 지연이나 상태 꼬임 방지.

### 5.4 오디오 마스터링: 리미터, 패닝, 피치 가변

- **리미터 (Limiter):** 여러 오디오 노드가 겹칠 때 발생하는 클리핑(치직거림) 방지 (`DynamicsCompressor` 노드 활용)
- **스테레오 패닝 (Stereo Panning):** 유닛의 전장 X 좌표를 `StereoPanner`(-0.85 ~ 0.85)에 연결
- **피치 가변 (Pitch Randomization):** 타격음 재생 시마다 ±40~90 cents 범위의 미세 피치 가변을 주어 반복 재생의 지루함 제거

### 5.5 지속음(Sustain SFX) 리소스 회수 처리

차지 샷이나 지속 빔처럼 키를 누르는 동안 계속 재생되는 오실레이터는 다음 3개 경로 전체에서 반드시 `stop()`을 호출해야 합니다.
1. 키/버튼 해제 시
2. 일시정지 또는 UI 오버레이 오픈 시
3. 씬/컴포넌트 언마운트 cleanup 단계

### 5.6 효과음 명확화를 위한 BGM 사이드체인 더킹 (Ducking)

타격음이 BGM과 동일한 대역에서 겹칠 때 볼륨을 무작정 올리기보다는, 타격음이 발생하는 순간 BGM의 볼륨을 수십 밀리초 동안 살짝 줄였다가(더킹) 천천히 복원시키는 방식을 권장합니다.

```js
export function duckBgm(amount = 0.4, dur = 0.4) {
  const t = c.currentTime;
  duckGain.gain.cancelScheduledValues(t);
  duckGain.gain.setValueAtTime(duckGain.gain.value, t);
  duckGain.gain.linearRampToValueAtTime(1 - amount, t + 0.04); // 서서히 낮춤
  duckGain.gain.exponentialRampToValueAtTime(1, t + dur);      // 자연스럽게 복원
}
```

---

## 6. 진행 및 성장의 설계

| 요소 | 설명 | 예시 A (로그라이크) | 예시 B (디펜스) |
|------|------|-------------------|-------------------|
| **등급 및 가중치** | 일반/희귀/전설 등장 확률 | Common 1.0 / Rare 0.5 / Legend 0.22 | 실력 및 플레이 성과로 가중치 상승 |
| **시너지 태그** | 동일 태그 2개 이상 보유 시 추가 등장 확률 곱연산 | 공격/생존/유틸리티 | 직업 및 특성 시너지 |
| **진화 "잭팟"★** | 단순 스탯 증가가 아닌 *행동 양식을 바꾸는 선택* | 9연발 부채꼴 발사, 벽 반사, 폭발 | 관통 화살, 화상 라인, 광역 감속 |
| **메타 진행** | 사망 후에도 이월되는 영구 업그레이드 | 재화 수집 → 영구 스탯 강화 | 별조각 → 성소 능력 해금 |
| **킬 콤보** | 단시간 연쇄 처치 시 보상 배율 적용 | 4연속 ×2 / 8연속 ×3 | 6연속 ×2 / 12연속 ×3 |

> **핵심 레슨:**
> 단순 수치(스탯 +10%) 증가보다 "화살이 이제 적 3명을 관통하고 폭발한다"처럼 플레이어의 행동 방식이 확 달라지는 **진화 잭팟 카드**가 플레이 만족도와 몰입에 훨씬 결정적인 영향을 미칩니다.

### 6.1 부가 활동 및 퀴즈 요소를 코어 루프의 "관문"으로 설계하기

교육 요소나 사이드 미니게임을 "하면 좋은 선택 요소"로 옆에 배치하면 플레이어들은 대부분 외면합니다. 진행 과정에서 자연스럽게 통과해야 하는 **핵심 관문(합성, 해금, 부활 지점)**으로 배치하는 것이 훨씬 효과적입니다.

### 6.2 성장 시스템 일원화

개별 유닛 레벨업과 등급 합성 시스템을 동시 탑재하면 플레이어는 더 직관적이고 재미있는 합성 쪽에만 몰입하고, 레벨업은 지루한 잡무로 느낍니다. 부차적인 스탯 배율 시스템을 제거하고 단일 합성 트리에 성장의 파워를 집중시키는 편이 좋습니다.

### 6.3 합성 트리의 한계 극복 (3세대 신화 구조)

최고 등급에 도달한 뒤 목표가 사라지는 문제를 방지하기 위해 1세대(기본) → 2세대(특수) → 3세대(신화) 구조로 트리를 확장합니다. 3세대 최상위 유닛은 단순 합성으로 도달할 수 없고, 특정 레시피 조건으로만 완성되도록 설계하여 장기 목표를 제시합니다.

### 6.4 리셋/리롤의 사후 악용 방지

퀴즈 창이나 관문 창을 무한히 닫았다 열면서 원하는 유용한 문제가 나올 때까지 리롤을 시도하는 편법은 **골드 감점이 아닌 준비 단계 조합 잠금(Lock)** 방식으로 차단합니다.

---

## 7. 게임필(Juice) & 조작성 체크리스트

적은 구현 노력으로 게임의 완성도와 손맛을 비약적으로 끌어올리는 팁 모음입니다.

- [ ] **잔상 체력바:** 피격 시 메인 HP바는 즉시 줄고, 뒤의 황색 잔상 바가 0.5초 지연 후 부드럽게 감소
- [ ] **히트 플래시 (Hit Flash):** 피격 시 유닛 순간 순백색 틴트 + 미세 스케일 업
- [ ] **콤보 팝:** 스프링 애니메이션(`cubic-bezier`)으로 튀어나오는 콤보 수치 UI
- [ ] **화면 피격 연출:** 플레이어/본진 피격 시 화면 테두리에 붉은 비네트 플래시
- [ ] **저체력 맥동 & 심장박동음:** 사운드와 visual pulse 동기화
- [ ] **상승 텍스처 (Damage Float):** 데미지/획득 자금 수치가 위로 떠오르며 페이드아웃
- [ ] **사거리 가이드 & 미리보기:** 사거리 표시 링, 다음 웨이브 유닛 구성 예고, 보스 HP 바
- [ ] **최초 1회 코치 팁 (Coach Chips):** 직관적 안내 칩 + 즉시 시작 버튼
- [ ] **결과 공유 카드:** Canvas 렌더링 기반 결산 스크린샷 다운로드/공유 기능

### 7.0.1 국소 플래시 예산 (Localized Flash Budget)

손맛은 화면 전체를 번쩍이게 해서 만들지 않는다. 일반 피격·조합·스킬·보스 경고는 **발동한 유닛, 카드, 경로, 또는 HUD 요소 주변의 작은 영역**만 강조한다.

1. **전체 화면 백색/색상 플래시는 금지한다.** 예외 없이 전장 전체를 덮는 오버레이 대신, 효과 중심의 오라·링·파티클·피해 숫자를 사용한다.
2. **한 번의 강조는 짧고 약하게:** 기본값은 400ms 이하, 낮은 알파, 전장 면적의 일부만 사용한다. 연속 이벤트는 새 플래시를 겹치지 말고 기존 효과를 갱신하거나 병합한다.
3. **위험 신호도 국소화한다.** 본진 피격·저체력·보스 경고는 성 체력 바, 성문, 보스 배너처럼 원인을 가리키는 위치에만 표시한다.
4. **의미는 다른 신호로 보강한다.** 사운드, 짧은 카메라 흔들림, 유닛 틴트, 떠오르는 수치로 타격감을 유지한다. 시각 자극을 키워 정보 전달을 대신하지 않는다.
5. **검증:** 모바일·저사양 화면에서 5초 이상 연속 전투를 확인하고, 플레이어가 발광 원인과 대상 위치를 즉시 말할 수 있어야 한다.

### 7.1 피드백 시스템의 일원화

이벤트 오디오, 시각 팝업, 알림 메시지는 개별 기능 코드에 인라인으로 흩뿌리지 말고, 이벤트 디스패처나 액션 큐 등 **중앙 통로 한 곳**에 통합하여 부착합니다.

### 7.2 키보드 및 예측형 UI 규칙

1. **키보드 중심 조작:** Enter(확인), Space(주 액션), Esc(취소/닫기), 방향키/Tab(타겟 전환), 숫자키(선택) 등 표준 바인딩 지원.
2. **단일 후속 행동 자동 진행:** 결과 노출 후(0.8~1.2초) 클릭 없이 자동 다음으로 넘어가 조작 피로 감소. (단, 실패 시에는 사유 확인을 위해 자동 진행 생략)
3. **입력창 포커스 이탈 대비:** 전역 핸들러에서도 주 액션을 수신할 수 있도록 설계하되 `ev.isComposing`(한글 입력 상태) 유무를 정확히 판별.

### 7.3 터치 기기 감지 및 레이아웃 최적화

- User-Agent 파싱 대신 `matchMedia('(pointer: coarse)')` 미디어 쿼리를 사용해 모바일/터치 입력을 정밀하게 구분합니다.
- 모바일 가상 조이스틱 생성 조건과 UI 지침 문구를 동일한 미디어 쿼리 변수로 관리하여 불일치를 방지합니다.

---

## 8. AI 협업 프롬프트 패턴 (실전 활용 지시문)

| 패턴 | 지시 예시 | 패턴의 효과 및 비고 |
|------|-----------|---------------------|
| **증상 중심 전달** | "이동 조작이 후반으로 갈수록 뻑뻑한 느낌이야. 증가량을 1/10로 줄이거나 곡선을 바꿔봐." | 원인 진단과 해결책 도출을 AI에게 일임 |
| **자율 위임형** | "네가 이제 심사위원이 되어서 게임에 빠진 추가 기능을 스스로 찾아내 구현해봐." | 미니맵, 콤보 연출, 진동 피드백 등을 AI가 자율 발굴 |
| **경쟁작 리뷰 분석** | "유사 장르 히트작 리뷰에서 재미 요소를 분석하고 우리 게임에 빠진 걸 진단해줘." | 전설 진화 특성 시스템의 아이디어를 도출 |
| **레퍼런스 이식** | "기존 저장소 X를 참고해서 전반적인 그래픽/사운드 퀄리티업 패스를 적용해줘." | 검증된 패턴을 신규 장르나 스택으로 이식 |
| **규칙 문서 승격** | "이번에 확정한 세계관/아키텍처 규약을 CLAUDE.md에 추가해줘." | 세션이 초기화되어도 개발 컨텍스트 일관성 유지 |
| **상시 스케줄링 위임**| "매일 아침 관련 게임, 신작, 오픈소스 소식을 조사해 추천 리스트를 정리해줘." | 일회성 조사를 **매일 구동되는 자동화 루프**로 승격 |
| **순차 실행 요청** | "한 번에 다 하지 말고 순서대로 하나씩 진행하며 검증해줘." | 실패 원인이 혼재되지 않도록 단계별 실측 및 검증 유도 |
| **다중 에이전트 감사**| "모든 UI/게임 경로를 N개 에이전트가 감사하고 교차 반증된 항목만 수정해줘." | 오탐을 교차 검증으로 거르고 실제 버그만 선별 해결 |

### 8.1 텍스트 중심 검증 증거 남기기 (터미널 SVG)

검증 결과를 저장소에 기록할 때 스크린샷 이미지(PNG/GIF) 대신, 테스트 커맨드의 stdout 출력을 **터미널 형태의 SVG 벡터 파일**로 생성해 문서에 첨부하면 용량이 획기적으로 줄어들고 `git diff`로 변경 사항을 손쉽게 추적할 수 있습니다.

---

## 9. 자동 검증 하네스 (Automated Harness)

AI가 헤드리스 브라우저 또는 백그라운드 환경에서 게임을 스스로 구동하고 플레이 검증을 수행하기 위한 전용 내장 도구들입니다.

### 9.1 하네스 핵심 구성 요소

1. **디버그 훅 (Debug Hooks):** `window.__game = { state, jump(wave), addGold(n) }` 형태로 상태 조회 및 강제 변경 API를 글로벌 노출 (`?debug` 쿼리 파라미터로 제어).
2. **`?rafshim` (rAF 대체 심):** 브라우저가 백그라운드 탭이나 헤드리스 환경으로 전환될 때 `requestAnimationFrame`이 스로틀되는 현상을 타이머(setInterval)로 우회.
3. **고정 타임스텝 (Fixed Timestep):** `while (acc >= 1/60) { tick(1/60); }` 형태로 루프를 돌려 프레임 드랍 환경에서도 게임 속도 및 시드 결과의 일관성 유지.
4. **시간 기반 연출 비활성화:** 명중 순간 `dt=0`으로 얼리는 히트스톱(Hit-stop)이나 슬로모션 등 결정론을 깨뜨리는 시각 연출은 고정 타임스텝 모드(봇 검증 시)에서 비활성화 처리.
5. **픽셀 검증 및 캡처:** WebGL Canvas의 `preserveDrawingBuffer` 옵션을 켜두고 `toDataURL()` 또는 `readPixels()`로 화면 차분을 추출하여 조명, 대비, 그래픽 효과를 검증.
6. **순수 함수 로직의 단독 CLI 검사:** 지형 생성이나 수치 조합처럼 렌더링이 불필요한 영역은 Node.js 환경에서 브라우저 없이 수백 개의 맵을 1초 만에 자동 검사.

### 9.2 오디오 노드 생성 카운팅 검증

헤드리스 환경에서는 오디오가 실제로 재생되는지 청음할 수 없으므로, `AudioContext` 메소드를 오버라이드하여 렌더링 시 생성되는 오디오 노드(오실레이터, 버퍼, 피드백 딜레이 등)의 수량을 계측하여 가상 검증합니다.

```js
// Playwright 등을 통해 페이지 스크립트 실행 전 오디오 노드 생성수 카운터 주입
window.__audio = { osc: 0, buf: 0, panner: 0 };
// createOscillator 등의 함수 호출 시 카운트 증가
```

### 9.3 콘텐츠 생성기의 역산 검산

수학 퀴즈나 퀘스트처럼 정답이 명확한 콘텐츠를 절차 생성하는 경우, 수만 개의 생성 결과를 기계적으로 평가(`evaluate(p.text) === p.answer`)하여 오답 문제나 0.3333... 처럼 입력 불가능한 정답이 포함되지 않았는지 사전에 자동 정적 검증합니다.

### 9.4 봇 구동 시 무음(Silent) 처리의 강제

하네스 봇을 통한 자동화 실행 시에는 `?mute` 또는 `?rafshim` 플래그 감지 시 무음 상태로 작동하도록 기본값을 잡되, 사용자의 `localStorage` 음소거 저장값을 덮어쓰지 않도록 전용 가드를 둡니다.

### 9.5 봇 데모(자동 시연)의 실게임 코드 공유

자동 시연 기능(Demo mode)을 별도의 스크립트 각본으로 작성하지 말고, 밸런스 봇의 플레이 판단 모듈(`src/bot.js`)을 그대로 공유하도록 설계합니다. 이렇게 하면 데모 자체가 항상 살아있는 E2E 회귀 테스트 역할을 수행하게 됩니다.

---

## 10. 첫 5분 설계 — 심사자와 신규 사용자의 이탈 방지

해커톤 심사나 사용자 리서치 결과를 분석하면 공통된 결론이 나옵니다. **심사자와 신규 유저는 게임을 길게 기다려주지 않으며, 초반 5분 동안 경험하는 코어 루프의 전달력이 전체 평가를 좌우한다는 사실**입니다.

| 항목 | 구현 내용 | 효과 |
|------|-----------|------|
| **즉시 시작 버튼** | 메인 타이틀에 "⚡ 즉시 게임 시작" 배치 | 인트로나 긴 튜토리얼을 건너뛰고 1초 만에 1웨이브 진입 |
| **1회성 코치 칩** | 첫 판에만 조작 안내 1줄 노출 (`localStorage` 캐싱) | 별도 튜토리얼 화면 없이 즉각적인 조작 이해 유도 |
| **핵심 목표 시각화** | 화면 상단에 최종 목표 (예: "신화 유닛까지 앞으로 N단계") 노출 | 게임의 차별화 포인트(잭팟)가 존재함을 시작부터 각인 |
| **자동 시연(관전) 모드** | 클릭 한 번으로 AI 봇의 플레이 관전 | 직접 플레이할 여유가 없는 심사자에게 시각적 완성도 전달 |

> **검증 팁:**
> "타이틀 화면의 즉시 시작 버튼 클릭 → N초 이내 코어 루프 HUD 표시 여부"를 CI 스모크 테스트 항목으로 묶어두면, 빌드 시 온보딩 경로가 깨지는 버그를 예방할 수 있습니다.

---

## 11. 상시 자동화 — 사람이 자는 동안 백그라운드에서 도는 루프

3장의 밸런스 봇을 일회성 검사 도구에 머무르게 하지 않고 **자동화 스케줄(CI/크론)에 연동**하여 지속적으로 품질을 관리합니다.

| 자동화 루프 | 실행 주기 | 핵심 기능 |
|-------------|-----------|-----------|
| **배포 게이트 스모크** | git push 마다 | 실브라우저 기반 온보딩 동선 + 디버그 훅 검사 (콘솔 에러 감지 시 배포 차단) |
| **야간 밸런스 회귀** | 매일 새벽 | 봇 N판 시뮬레이션 → 사망 분포를 `baseline.json`과 대조하여 이탈 시 이슈 자동 발행 |
| **일일 봇 실주행** ★ | 매일 새벽 | 일일 시드 게임을 봇이 구동하여 시뮬레이션한 플레이 기록을 정적 파일로 생성/커밋 |
| **아침 트렌드 스카우트** | 매일 아침 | 뉴스, 게임잼, 신작 트렌드를 조사하여 3-Gate로 필터링한 상위 3~5개 추천 리스트 생성 |
| **문서 데이터 업데이트** | 주기적 | git 커밋 로그 및 회고 데이터를 분석하여 통계 및 문서 차트를 최신화 |

### 11.1 시뮬레이션 결과물의 게임 콘텐츠화

밸런스 봇의 시뮬레이션 결과(`public/ghost/<날짜>.json`)를 정적 JSON 파일로 커밋해 두면, 서버나 DB 구축 없이도 "오늘 AI 플레이어의 기록"과 경쟁하는 **일일 챌린지 콘텐츠**로 활용할 수 있습니다.

### 11.2 크론 자동화 5대 원칙

1. **멱등성 보장:** "당일 기록이 이미 존재하면 즉시 종료"하도록 설계하여 중복 커밋 차단.
2. **타임존 명시:** 서버의 UTC와 게임 기준 시간대(KST 등)의 불일치 방지.
3. **실패 시 인라인 조용한 폴백:** 봇 구동이 실패하면 빈 정적 파일을 생성하지 않고 이전 폴백 상태 유지.
4. **결과 분리:** 비정상 런타임 종료(exit 1)와 정상적인 밸런스 이탈(exit 2)을 분리하여 후자일 때만 알림 이슈 발행.

---

## 12. 새 게임 부트스트랩 체크리스트 (복사하여 활용)

```
[ ] data.(js|ts)   — 모든 밸런스 수치를 단일 파일로 구성
[ ] engine.(js|ts) — DOM/렌더러 비의존 pure tick(state, dt) → events[], RNG 주입식 설계
[ ] 시드 RNG (mulberry32) 도입 — 실게임=Math.random / 봇=시드 난수
[ ] render.(js|ts) — state 렌더링 + events 기반 시각 이펙트 (블롭 그림자 포함)
[ ] sfx — tone() + noise() 두 도구 기반, 성공=상승/실패=하강, 히트 레이트리밋(45~70ms)
[ ] music — 스텝 시퀀서 및 상태별 BGM 트랙
[ ] scripts/balance-bot — 가상 플레이어 3종 × N판 시뮬레이션 → 사망 분포 도출
[ ] scripts/baseline.json + `check` 회귀 게이트 구축
[ ] Push-Your-Luck 요소 포함 시 결정점 손익분기 기대값 검사 (3.7)
[ ] 이종 언어/런타임 간 수치 대조 정규식 검사 (2.5)
[ ] 에셋 0개 규칙 검증 테스트 추가 (부록 B)
[ ] window.__game 디버그 훅 + ?rafshim + 고정 타임스텝 + 연출 전용 훅 (9장)
[ ] 게임필: 잔상 체력바, 히트 플래시, 콤보 팝, Floating 데미지 숫자
[ ] 키보드 전 기능 조작 (Enter/Space/Esc/Tab/단축키) + UI 핫키 아이콘 라벨 표기
[ ] 예측형 UI: 유일한 다음 행동 자동 진행, 반복 루프 Enter 1버튼, 되돌리기 동선 차단
[ ] 핵심 스탯 카드 표시 + 호버 툴팁 파생 수치 안내
[ ] 행동 양식을 바꾸는 진화 "잭팟" 특성 최소 1종 탑재
[ ] 사망 시 이월되는 영구 메타 진행 재화 설계
[ ] 자동 품질 조절 (실측 FPS 기반 가벼움 모드 자동 강등)
[ ] 첫 5분 설계: 즉시 시작 버튼 + 1회성 코치 칩 + 핵심 목표 노출 + 결과 PNG 캡처
[ ] CI 스모크 게이트 (첫 방문자 동선 + 콘솔 에러 0, 의도된 404 필터링)
[ ] 야간 밸런스 회귀 크론 (이탈 시 GitHub 이슈 자동 생성)
[ ] 일일 봇 실주행 → 시뮬레이션 기록 정적 JSON 커밋 & 멀티플레이어 경쟁 콘텐츠 활용
[ ] 아침 트렌드 스카우트 루틴 (저장소 읽기 → 3-Gate 필터 → Top 3~5 추천)
[ ] CLAUDE.md — 아키텍처, 밸런싱 규칙, 검증법, 백로그 후보 정리
[ ] Public 저장소 및 GitHub Pages 자동 배포 배선
```

> **우선순위 가이드:**
> 상단 절반(엔진· RND RNG · 봇 · 게임필)은 **프로젝트 첫째 주**에 구축하고, 하단 자동화 4종은 **코어 루프가 충분히 재미있어진 직후**에 연결하세요.

---

## 부록 A. 왜 이 방법론은 "장르 무관"인가

| 축 | 로그라이크 (dungeon100) | 디펜스 (defenehero) | 소셜 (giwa-village) | 공통 추상 개념 |
|----|------------------------|---------------------|--------------------|----------------|
| **코어 압박** | 심층으로 진입할수록 강해지는 적 | 웨이브가 진행될수록 몰려오는 enemy | 주간 이벤트 및 던전 보스 | 진행에 따른 위협 및 난이도 가속 |
| **성장** | 드래프트 선택지 기반 아이템 빌드 | 소환, 조합, 강화 | 칭호, 장신구, 온체인 문양 | 무작위성 + 기획적 결정 |
| **진화 잭팟** | 진화 스킬 카드 | 전설 특수능력 획득 | — (추후 개선 과제) | 행동 양식 자체를 바꾸는 순간 |
| **검증** | 사망 층 분포 측정 | 도달 웨이브 분포 측정 | 결정점 손익분기 검사 (3.7) | 선택지가 유의미하게 작동하는가 |
| **손실/위험** | 던전 사망 시 리셋 | 성 함락 리셋 | 함정 실패 시 잠정 손실 | 잃을 위험이 있어야 선택의 긴장이 형성됨 |

장르와 테마는 달라도 오른쪽 열의 **공통 추상 개념**은 동일하게 유지됩니다. 새 게임을 구상할 때 이 공통 추상 열의 질문들을 채우는 방식으로 방법론을 적용할 수 있습니다.

---

## 부록 B. 라이선스 안전성 (License-Safe Rules)

외부 자산 및 라이브러리 반입 시 라이선스 사고를 구조적으로 방지하기 위한 원칙과 체크리스트입니다.

### B.1 외부 자산 반입의 기본 원칙

- **기본 원칙:** 이미지/사운드는 코드로 직접 생성(Procedural generation)하는 것을 기본으로 유지합니다.
- **반입 허용 기준:** 외부 자산을 도입할 경우 "원장(`assets.json`) 없는 반입 금지" 정책을 엄격히 적용합니다.

### B.2 애셋 종류별 반입 정책

| 종류 | 정책 | 사유 및 비고 |
|------|------|--------------|
| **폰트 (Font)** | OFL/CC0 라이선스만 허용 | `CREDITS.md` 명시 + 시스템 폰트 폴백 필수 |
| **그래픽 (3D/2D)** | 허용하되 아트 스타일 통일 우선 | 스타일 파편화로 인한 비주얼 조잡화 방지 |
| **사운드 (Audio)** | BGM은 코드 합성 유지, 단발 효과음만 반입 허용 | 효과음은 실사 반입이 가성비 최고이며 BGM은 동적 변주에 합성이 필수적임 |

### B.3 라이선스 원장 (Ledger) 4대 규칙

1. **기계가 읽을 수 있는 파일로 원장 작성 (`assets.json`):** 출처 URL, 저작자, 라이선스 명시 후 `CREDITS.md` 자동 생성.
2. **원본 바이너리는 수정 없이 보관:** 게인/피치 조절 등은 코드에서 실행하여 원본 체크섬 검증 유지.
3. **`npm run assets` CLI로 반입 검증:** 원장 기반 지문 대조 자동화.
4. **폴백(Fallback) 보장:** 파일 404/로드 실패 시에도 시스템 폰트나 합성음으로 안전하게 폴백되도록 설계.
