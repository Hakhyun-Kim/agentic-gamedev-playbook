# 🗳️ 검토 대기열 (Review Queue)

주간 회고 에이전트가 **자기 판단으로 처리하지 않고 사람에게 남긴 것**들입니다.
회고는 근거가 분명한 추가·정정은 곧바로 커밋하고, 아래 성격의 항목만 여기에 적습니다.

> ⚠️ 문서를 고칠 때는 **`skills/agentic-gamedev/references/` 의 해당 장 파일**을 고치고
> `npm run build` 로 `PLAYBOOK.md`(합본, 생성물)를 재생성하세요. 합본을 직접 고치면
> `npm test` 가 실패합니다. 장을 신설했다면 `scripts/playbook-source.mjs` 의 `CHAPTERS` 와
> `SKILL.md` 라우팅 표에도 추가해야 합니다(테스트가 검사합니다).

| 남기는 기준 | 왜 사람이 정해야 하나 |
|---|---|
| **문서 골격 변경** (장 신설·삭제·재구성) | 방법론의 구조는 저자의 관점이다 |
| **기존 규칙과 충돌** | 둘 중 무엇이 맞는지는 방향 결정 |
| **사례 1개뿐인 일반화** | 표본이 모자라 규칙으로 승격할지 판단 필요 |
| **공개 적합성이 애매** | 대회·제출·비공개 저장소에서 온 내용 |
| **입장 전환** ("하지 마라" → "해도 된다") | 이전 판단을 뒤집는 결정 |
| **MAJOR 버전** 급 변경 | 사용자에게 영향이 큰 변화 |

처리 방법: 항목을 읽고 **"1번 반영해줘"** 처럼 말하면 세션에서 적용합니다.
필요 없다고 판단하면 그 줄을 지우면 됩니다 — 회고는 이 파일에 이미 있는 항목을 다시 적지 않습니다.

---

## 대기 중

### 9. 지형 경계의 직선을 노이즈로 지우는 기법 — 4장에 넣을지, 넣는다면 어느 소절에

- **날짜**: 2026-08-06
- **무엇을 정해야 하나**: "서로 다른 지형(잔디↔모래)이 만나는 자리는 직선이면 인공적으로
  보이니, 월드 좌표로 노이즈를 뽑아 경계를 흔들라"는 기법을 4장에 넣을지. 후보 위치:
  4.1(빛·그림자·톤매핑) 옆 절차 텍스처 항목의 확장, 아니면 새 소절.
- **근거**: defenehero 커밋 `bdcaadf`(2026-08-05, `src/render3d.js` +44) — `_grassShoreEdge`
  가 `onBeforeCompile`로 셰이더에 `varying vec2 vShoreW`(월드 xz)를 주입하고,
  `shoreHash`/`shoreNoise`(value noise)로 경계를 흔들어 해안선을 그립니다. 주석에
  "바다의 물가 선과 같은 수법"이라 적혀 있어 **같은 저장소 안에서 두 번째 적용**입니다.
  주파수 0.33, 진폭 ±1.0, 그리고 "짧게만 문다 — 뭍 쪽으로 너무 파이면 물가 잔디잎이
  뜬다"는 캐빗까지 커밋에 남아 있습니다.
- **선택지와 추천**: 기법 자체는 작고 이식성이 높지만(그리드로 만든 모든 경계에 통함),
  **아직 게임 하나 안에서만 검증**됐습니다(대기열 기준표의 "사례 1개뿐인 일반화").
  다른 프로젝트에서 같은 손질이 다시 나오면 4.1의 절차 텍스처 항목에 두세 줄로 승격을
  추천합니다. 지금 넣는다면 "직선 경계는 인공적으로 보인다 → 월드 좌표 노이즈로 흔들되,
  침범 폭에 상한을 둬라(다른 레이어가 뜬다)" 정도가 적정 분량입니다.
- **함께 미룬 것**: defenehero `19d32c9`(후반 목표를 도감·옷장·잔치 세 겹으로) — 6.0.6
  ("합성 게임이 중반 이후 애매해지는 이유")과 겹치는지 판단이 필요해 이번 주 상한(3건)
  밖으로 넘겼습니다.

### 4. 없앤 확인창과 같은 모양으로 남은 "진짜 선택"은 세계 오브젝트로 옮겨라 — 새 소절 신설 여부

- **날짜**: 2026-07-28
- **무엇을 정해야 하나**: "자동 진행(7.2)으로 확인창을 없앴는데, 일부 경우엔 진짜 선택지가
  남아 있어 그 화면이 가끔 다시 뜬다"는 상황에서, 화면(모달)이 아니라 **월드 오브젝트에
  선택을 묻는 것으로 옮기는** 패턴을 문서에 넣을지, 넣는다면 어디에(7.2 예측형 UI 근처
  작은 항목으로, 아니면 새 소절로).
- **근거**: dungeon100 커밋 `29dfb97`(2026-07-27) — "내려가기 확인창"을 자동 진행으로
  없앤 뒤(2026-07-26), 갈림길이 열린 층에서만 **똑같이 생긴 화면이 다시 떠서** "지워진
  버그가 되살아났다"는 신고를 받았다. 원인은 렌더 버그가 아니라 의미였다: 그 화면만은
  진짜 선택(어느 문으로 갈지)이라 자동 진행 대상이 아니었는데, 사라진 화면과 생김새가
  같아 구분이 안 됐다. 처방: 선택을 화면에서 **던전 바닥의 붉은 포털(출구 근처 3~5칸,
  결정적 배치·난수 0회)**로 옮겨 "어느 포털에 몸을 넣는지가 곧 대답"이 되게 했다.
- **선택지와 추천**: 아직 dungeon100 한 곳에서만 쓰였다(사례 1개뿐인 일반화 — 대기열
  기준 표 참조). 이번 회고에서 이미 2개 변경(4.4·9.6)을 반영해 상한에 닿았고, 판단
  자체도 "이 정도 UX 통찰이 코드 기법만큼 재사용 가능한가"라는 취향의 문제라 사람이
  보는 게 낫다고 판단했다. 반영한다면 7.2(예측형 UI) 근처에 "자동 진행이 만든 빈 자리에
  진짜 선택이 남으면, 그 선택은 반드시 없앤 확인창과 다르게 생기게 하라 — 가장 쉬운
  방법은 화면이 아니라 세계(오브젝트를 향해 걷기)로 옮기는 것" 정도의 짧은 항목으로
  추천.

### 5. 첫 방문자 온보딩 — "딱 한 번 묻기 + 빈 자원 상태로 강등" 패턴을 10장에 추가할지

- **날짜**: 2026-07-29
- **무엇을 정해야 하나**: 판단이 필요한 항목은 아니고 **이번 회고의 2개 반영 상한을 넘겨서**
  미룬 것이다(대기열 기준 표의 "사람이 정해야 하나"에 해당 안 함 — 순수 분량 조절). 다음
  회고(또는 이 문서를 읽는 세션)가 그대로 반영해도 된다.
- **근거**: giwa-village 커밋 `a8b827f`(2026-07-29) — 첫 방문자 이탈이 가장 흔한 문제였는데,
  촌장이 **한 번만** "대신 걸어서 보여줄까?"를 묻고 답을 로컬 저장소에 남겨 다시 안 묻는다.
  첫 방문 판정은 **온보딩 마커 두 종류가 전부 없을 때**로 잡아, 이미 시작한 사람에게 처음인
  척 다시 묻지 않는다. 지갑 잔액이 비어 있으면(신규 방문자 대부분) 쓰기를 하나도 안 하는
  **구경 모드**로 내려가 코어 콘텐츠를 걸어서 보여준다 — "자원 없는 상태 앞에 세워 두는
  것"이 가장 나쁜 첫인상이라는 진단. 시연 중엔 다른 온보딩 안내(퀘스트 로그)가 비켜준다.
  `scripts/smoke-boot.mjs`에 빈 localStorage 기준 4가지(카드가 뜨는가·한 번 답하면 다시
  안 묻는가·수락 시 그 자리에서 시작하는가·ESC로 멈추면 안내가 돌아오는가) 게이트로 고정.
- **선택지와 추천**: 10장(첫 5분)의 "관전 모드" 행을 확장하거나 그 아래 짧은 소절로.
  이미 있는 "⚡ 바로 시작"(강제하지 않고 건너뛸 자유를 준다)과 결이 같아 새 장 없이도
  들어간다 — **반영 추천**. 문구 후보: "온보딩 질문은 **한 번만** 하고 로컬에 남겨라
  (판정은 관련 마커 전부 부재일 때만) · 신규 방문자는 자원이 비어 있을 확률이 높으니
  **쓰기 0건으로 강등되는 구경 모드**를 준비하라 — 자원 앞에 세워 두는 것보다 걸어서
  보여주는 쪽이 낫다."

### 6. git log 기반 리포트의 날짜 그룹핑 — "인접 비교 대신 날짜 Map" 캐빗을 11장에 추가할지

- **날짜**: 2026-07-29
- **무엇을 정해야 하나**: 이것도 판단 항목이 아니라 **분량 조절로 미룬 것**이다.
- **근거**: dungeon100 커밋 `0e84d1b`(2026-07-28) — 개발 일지 자동 생성 스크립트
  (`scripts/journal-data.mjs`, 11장 "문서 데이터 자동 생성" 행의 실제 구현체)가 `git log`의
  **인접 커밋만 비교**해 같은 날짜를 하나로 묶고 있었다. `git log` 순서는 작성 시각과
  어긋날 수 있는데(자동 커밋이 다른 시각대에 섞여 들어오는 경우 등) 실제로 같은 날짜가
  두 줄로 갈라져 차트 날짜 축이 뒤엉켰다. 고친 방식은 **날짜별 Map으로 모은 뒤 날짜순
  정렬** — "인접하다"가 아니라 "같은 키"로 묶는, 흔한 종류의 group-by 실수.
- **선택지와 추천**: 11장 "문서 데이터 자동 생성" 행에 한 줄 각주로 — "git log를 인접
  비교로 그루핑하지 마라(자동 커밋이 섞이면 작성 순서가 날짜 순서와 어긋난다) — 날짜를
  키로 한 Map에 모아라." 새 소절까지는 과하다고 판단, 기존 행 보강 정도로 **반영 추천**.

### 7. 상위 시스템이 없는 기능은 절반 구현 대신 설계 스케치로 남겨라 — 어디에 적을지

- **날짜**: 2026-07-30
- **무엇을 정해야 하나**: "3-Gate는 통과하는 아이디어인데, 그 기능이 걸릴 상위 시스템
  (트리거·모드·화면 흐름) 자체가 아직 없다"는 상황에서, 코드를 절반만 만드는 대신
  **설계 스케치(안전 제약 + 최소 시험 단위 후보 + 다음 세션이 바로 쓸 순수 함수 스케치)만
  문서에 남기고 코드는 안 짜는** 판단 패턴을 문서화할지, 넣는다면 어디에(1장 3-Gate
  근처 — "통과해도 구현을 미루는 경우"로, 아니면 8장 협업 원칙 — "미완성 절반 구현보다
  스케치가 낫다"의 구체화로).
- **근거**: dungeon100 커밋 `53a0bdd`(2026-07-29) — 시각 왜곡(지각 착시) 기능을 저주
  단계·무한 모드 백로그에 얹으려다, **저주 단계 시스템 자체가 저장소에 없어** "언제
  켜지는가"를 설명할 상위 맥락이 없다고 판단하고 멈췄다. 아무 기존 층에나 끼워 넣으면
  ①일반 플레이를 갑자기 헷갈리게 만드는 하향 조정이거나 ②상위 개념과 무관한 장식이
  되어 3-Gate ②(결정을 더하는가)를 통과하지 못한다고 봤다. 대신 남긴 것: 지켜야 할
  안전 제약(HUD 표시값만 왜곡, DEV 훅이 돌려주는 실제 좌표·충돌·시뮬레이션 값은 절대
  불변 — 안 지키면 검증 하네스가 읽는 '진실'이 오염된다), 최소 시험 단위 후보 3개
  (규모순), 다음 세션이 바로 쓸 순수 함수 스케치(`illusionOffset(seed, t)` — 별도
  시드로 렌더 레이어만 왜곡, 판정 로직과 완전히 분리).
- **선택지와 추천**: 사례 1개뿐인 일반화라 대기열 기준에 해당한다. 이번 회고는 이미
  2개 변경(9.9·6.2) 상한에 닿아 판단 자체를 미룬다. 반영한다면 1장(3-Gate) 끝에
  "3-Gate를 통과해도, 그 기능이 걸릴 상위 시스템이 아직 없으면 절반 구현하지 말고
  스케치만 남겨라 — 안전 제약(검증 하네스가 읽는 '진실'은 렌더 레이어가 절대 못 건드리게)과
  최소 시험 단위 후보를 함께 적어 다음 착수를 준비하라" 정도의 짧은 항목을 추천.
  8장 "미완성 절반 구현보다 스케치가 낫다"는 원칙이 이미 있다면 그 옆에 실측 사례로
  붙이는 편이 더 가벼울 수 있다 — 두 후보 중 어느 쪽이 나은지는 8장 현재 문구를
  먼저 확인한 사람이 정하는 게 낫다고 판단해 대기열로 남긴다.

### 8. "100% 안전한 버튼"에 예외 유닛으로 카운터플레이를 심는 패턴 — 6.1(CC 상한) 옆에 붙일지, 새 항목으로 둘지

- **날짜**: 2026-07-31
- **무엇을 정해야 하나**: 6.1에는 이미 "CC(정지·기절)에는 반드시 상한을 — 보스 저항(×0.35)
  + 재적용 면역"이라는 원칙이 있다. 이번 사례는 그 원칙을 **플레이어가 쓰는 시간 정지형
  방어 자원**(적・탄막을 멈추는 버튼)에도 그대로 적용한 것 + 구현 방법이 한 가지 늘었다.
  기존 원칙의 실측 사례로 6.1에 한 줄 보태는 정도로 충분한지, 아니면 "예외 유닛의
  구현은 조건문을 흩뿌리지 말고 지역 변수를 다시 그림자 씌워라"는 별도 구현 기법으로
  9장(하네스, 기존 히트스톱 dt 항목 옆)에 새로 넣을 가치가 있는지 판단이 필요하다.
- **근거**: dungeon100 커밋 `2fcdffc`(2026-07-31) — "찰나"(처치 게이지로 얻는 시간 동결
  버튼)는 도입 이후 늘 100% 안전한 선택이었다(적・보스・탄막이 전부 멈추므로). 정예
  하나(출구 수문장)만 이 효과에 저항하게 만들어 "지금 얼려도 되나"라는 실시간 판단을
  새로 만들었다. 구현은 기존 `{ const dt = frozenNow ? 0 : worldDt; ... }` 블록(적 루프
  전체를 한 번에 가리는 방식)의 이름을 `frozenDt`로 바꾸고, **적 루프 안에서만**
  `const dt = e.elite ? worldDt : frozenDt;`로 다시 그림자를 씌워 그 아래 로직(walk·
  hitCd 등)은 한 줄도 안 건드리고 정예만 실제 시간으로 움직이게 했다. 접촉 피해 게이트도
  `!frozenNow` → `!frozenNow || e.elite`로 한 곳만 넓혔다. 추가로 ①얼어붙은 채 정예에게
  맞으면 남은 지속시간을 깎는 "타이머 도둑"(`FREEZE_STEAL=0.35초`) ②발동 순간 근처에
  살아있는 정예가 있으면 붉은 균열 이펙트로 미리 경고(맞고 나서 깨닫기보다 쓰기 전에
  안내)를 덧붙였다. 아이디어 출처는 게임 리뷰 조사(Ascend to ZERO — "일부 몬스터는
  얼어붙은 시간 속에서도 공격하고 타이머를 훔친다").
- **선택지와 추천**: 사례 1개뿐인 일반화라 대기열 기준에 해당한다. 이번 회고는 이미
  2개 변경(7.2.3 터치 감지·5.0 오디오 제스처 편승) 상한에 닿아 판단을 미룬다. 반영한다면
  ①6.1 CC 상한 항목 옆에 "같은 원칙이 CC가 아닌 자원(시간 정지 등 방어 버튼)에도
  적용된다"는 실측 각주로 가볍게, 또는 ②9장 히트스톱 dt 항목 옆에 "전역 dt/시드 셰도잉에
  예외 유닛을 추가할 땐 조건문을 소비처마다 흩뿌리지 말고, 그 유닛의 루프 안에서만 지역
  변수를 다시 그림자 씌워라"는 구현 기법으로. 둘 다 근거는 확실하지만 어느 장에 놓을지는
  기존 문구를 먼저 읽은 사람이 정하는 게 낫다고 판단해 대기열로 남긴다.

---

## 처리 완료

### ✅ 3. 에셋 반입 정책 — **부록 B 갱신 + 4.3·5.4·9.2 신설** (2026-07-27)

기준상 "입장 전환"이라 대기열에 남길 항목이었지만, 사용자가 직접 반영을 지시했습니다.
**입장이 뒤집힌 것은 아닙니다** — 부록 B는 원래도 "허용하기로 했다면"이라는 조건절을 갖고
있었고, 이번에 더한 것은 그 조건을 **강제 가능한 형태**로 만든 규율(원장 → 생성 문서,
바이너리 원본 유지, `npm run assets` 재현, 폴백 필수)과 테스트 예시(고아 파일·허용
라이선스·표기 의무·용량 예산)입니다. 기본값은 여전히 "코드로 만든다"로 못 박았습니다.

근거는 기와장터(GIWA Village) 2026-07-27 작업입니다: CC0 효과음 16개·CC0 1K 텍스처
4장·OFL 한글 폰트 2종(서브셋 181개)을 4.5MB로 반입하면서, 배경음은 절차 생성 그대로
두고 합성이 약한 자리만 채웠습니다. 함께 얻은 기법은 사진 없이도 유효해서 각 장에
따로 넣었습니다 — 4.3(색이냐 결이냐 · 늦게 도착한 map 재컴파일), 5.4(Karplus–Strong ·
절차 IR 컨볼루션 · 귀는 아바타에 · 타격 3겹 · 발소리는 거리로), 9.2(AudioContext를
앱보다 먼저 감싸 노드를 센다).

**저자가 확인할 것**: 부록 B 첫 두 줄의 표현이 방법론의 0장 전제와 어긋나지 않는지.
어긋난다고 보면 그 두 줄만 되돌리면 됩니다(나머지 기법 장은 반입 여부와 무관합니다).

### ✅ 1. 히트스톱 × 고정 타임스텝 충돌 → **§9에 반영** (2026-07-26)

추천대로 §9 자동 검증 하네스에 넣었습니다(§7 체크리스트는 이유를 적기 어렵다는 판단에 동의).
"시간을 조작하는 연출은 봇 검증 중에 꺼라" 항목 — 가드 코드 한 줄 + 슬로모션·화면 정지까지
일반화. **근거의 수치는 정정했습니다**: 대기열에 적힌 "0.22/0.55/0.4/0.9초"는 정지 시간이 아니라
세기(power) 값이고, 실제 정지는 `0.02 + power×0.07`(캡 0.09)로 **35~83ms**입니다.

### ✅ 2. 터미널 출력 SVG 렌더 → **§8.1 신설** (2026-07-26)

"사례 1개뿐이라 보류"가 기준상 맞는 판단이었지만, 사용자가 반영을 선택해 소절로 넣었습니다.
0장 원칙("복잡성을 에셋이 아니라 코드에")이 문서 증거에도 적용된다는 틀로 일반화.
**수치 정정**: `test-local.svg`는 10.6KB가 아니라 **12KB**(12,475바이트), `demo.gif`는 **11.8MB**
(12,367,012바이트) — 약 1000배 차이.

### 1. 히트스톱(hit-stop)과 고정 타임스텝 봇 검증의 충돌 — 어디에 어떻게 적을지 (원문)

- **날짜**: 2026-07-26
- **무엇을 정해야 하나**: "타격 프레임에 dt=0을 주는 히트스톱" 같은, *시간 자체를 조작하는*
  게임필 기법을 문서에 추가할 때 위치를 어디로 할지. 후보: ①§7 게임필 체크리스트에 항목만
  추가 ②§9 자동 검증 하네스(고정 타임스텝 항목)에 "결정론을 깨는 이펙트" 경고로 추가
  ③둘 다.
- **근거**: dungeon100 커밋 `7d1dcf20`(히트스톱 도입 — 타격 0.22/치명타 0.55/처치 0.4/정예 0.9초
  dt=0)과 CLAUDE.md의 "고정 dt(봇 검증) 중에는 끈다" 메모. 실제로 시간 조작형 이펙트가
  결정론적 봇 하네스(§2.2·§9)와 충돌할 수 있음을 보여주는 사례.
- **선택지와 추천**: §9에 "게임필 이펙트가 dt·시드 소비 순서를 바꾸면 고정 타임스텝 봇 하네스
  아래서는 꺼라"는 한 줄 경고로 추가하는 쪽을 추천(§7 체크리스트는 목록이라 이유를 설명하기
  어려움). 이번 주 3개 변경 상한을 넘어 다음 회고로 넘김 — 사람 판단이 필요해서라기보다
  분량 조절 목적.

### 2. 터미널 출력을 SVG로 렌더해 문서 근거로 첨부하는 패턴 — 새 소절 신설 여부

- **날짜**: 2026-07-26
- **무엇을 정해야 하나**: "명령 실행 결과를 스크린샷 대신 텍스트 기반 SVG로 렌더해 README/기술
  문서에 첨부"하는 기법을 플레이북에 넣을지, 넣는다면 어디에(§8 AI 협업 프롬프트 패턴 근처에
  작은 항목으로, 아니면 새 소절로).
- **근거**: giwa-village 커밋 `bf4ddfeb`(`scripts/render-run.mjs` 신설 — 명령 stdout을 터미널
  모양 SVG로 렌더. 스크린샷 대신 SVG를 고른 이유: 텍스트라 diff 가능·해상도 무관·용량 작음
  `test-local.svg` 10.6KB vs `demo.gif` 12MB). "산출물을 텍스트로 유지"라는 이 방법론의 핵심
  사상(§0)과 결이 같다.
- **선택지와 추천**: 아직 giwa-village 한 곳에서만 쓰였다(사례 1개뿐인 일반화 — 대기열 기준
  표 참조). 다음 프로젝트에서 같은 패턴이 다시 나오면 §8 근처에 "실측 근거를 텍스트로
  남겨라" 한 줄로 승격을 추천. 지금 바로 넣기엔 아직 이르다고 판단해 대기열로 남김.
