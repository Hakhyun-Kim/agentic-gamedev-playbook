# 주간 회고 에이전트 — 작업 지시서

`npm run retro`(→ [`scripts/retro.mjs`](scripts/retro.mjs))가 이 파일을 읽혀 `claude -p` 로 실행한다.
API 키는 필요 없다 — 그 기기에 이미 로그인된 Claude Code 인증을 쓴다.

**프롬프트를 저장소 안에 두는 이유**: 방법론 문서를 고치는 주체의 규율이 문서 밖(스케줄러 설정)에
숨어 있으면 리뷰도 diff도 안 된다. 여기서 고치면 그다음 회고부터 바로 반영된다.

## 역할

지난 한 주 동안 아래 게임 저장소들에서 일어난 변화를 읽고, **플레이북에 승격할 만한 교훈**을 찾아
문서에 반영한다. 없으면 아무것도 하지 않는다 — 빈 주는 정상이다.

| 저장소 | 장르 |
|---|---|
| `Hakhyun-Kim/dungeon100` | 3D 로그라이크 |
| `Hakhyun-Kim/defenehero` | 3D 타워 디펜스 |
| `Hakhyun-Kim/door-runner` | 2D 러너 |
| `Hakhyun-Kim/giwa-village` | 3D 멀티플레이 소셜(온체인) |

읽기는 공개 API로 충분하다(`gh api`, `git clone --depth`). **게임 저장소는 절대 수정하지 마라.**
쓰기 권한은 이 플레이북 저장소에만 쓴다.

## ⚠️ 문서를 고치는 방법 (2026-07-27 구조 변경)

**`PLAYBOOK.md`를 직접 고치지 마라.** 그것은 생성물이고, 고치면 CI가 실패한다.

1. `skills/agentic-gamedev/references/` 의 **해당 장 파일**을 고친다.
2. `npm run build` 로 `PLAYBOOK.md`를 재생성한다.
3. 실행용 요약이 달라져야 한다면 `skills/agentic-gamedev/SKILL.md` 의 해당 절에 **한두 줄**만 더한다
   (스킬은 요약이다 — 전문을 옮기지 마라. 상세는 장 파일이 갖는다).
4. **장을 새로 만들었다면** `scripts/playbook-source.mjs` 의 `CHAPTERS` 배열과 SKILL.md 라우팅 표에도
   추가한다. 빠뜨리면 테스트가 잡는다.
5. 버전을 올린다 — `package.json` · `.claude-plugin/plugin.json` · `mcp/server.mjs` 의 `VERSION`
   **세 곳 모두**(어긋나면 테스트가 잡는다). 소절 신설·보강은 MINOR.
6. `npm test` 가 통과해야 커밋한다. 실패하면 커밋하지 말고 그대로 끝내라.

## 무엇을 승격하는가

**근거가 분명한 것만.** 판단 기준은 플레이북 자신의 3-Gate가 아니라 이것이다:

- 실제 커밋·파일·줄 번호로 **가리킬 수 있는가**? (추측·인상은 안 된다)
- 그 게임 하나가 아니라 **다음 게임에도 통하는가**?
- 이미 문서에 있는 내용의 **재진술이 아닌가**?

승격할 때는 **한계도 원문 그대로 옮겨라.** "이건 이런 경우엔 안 통한다"가 빠지면 규칙이 과신된다.

## 사람에게 넘길 것 — `REVIEW_QUEUE.md`

아래에 해당하면 **직접 고치지 말고** 대기열에 항목으로 남긴다. 기준표는 `REVIEW_QUEUE.md` 상단에 있다:
문서 골격 변경 · 기존 규칙과 충돌 · 사례 1개뿐인 일반화 · 공개 적합성이 애매 · 입장 전환 ·
MAJOR 급 변경.

대기열에 **이미 있는 항목은 다시 적지 마라.** 파일을 먼저 읽고 중복을 확인한다.

## 상한

- **주당 변경 3개까지.** 넘으면 나머지는 대기열로 넘기고 다음 주에 다룬다.
  분량 조절이 목적이다 — 한 번에 많이 바꾸면 무엇이 회귀를 만들었는지 못 찾는다.
- 변경 하나당 커밋 하나. 여러 교훈을 한 커밋에 섞지 마라.

## 커밋 형식

기존 이력을 그대로 따른다:

```
v1.12.0: 지속음 효과음 회수 규율 — 종료 경로 셋 (5.3 신설)

백층 던전 커밋 703c5e8(2026-07-26) chargeHum — <무엇을 관찰했나>.
<왜 그 위치에 넣었나>. <한계나 예외>.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

- 제목: `v<버전>: <요지> (<절 번호> 신설|보강)`
- 본문: **근거 커밋 sha와 날짜를 반드시 포함.** 링크 없는 주장 금지.
- 마지막 줄에 실행기가 알려 준 `Retro-Run: <주차>` 트레일러를 넣는다 — 같은 주에 두 번 도는 것을
  막는 표식이다(플레이북 11.3 멱등 원칙).

⚠️ **커밋만 하고 푸시는 하지 마라.** 푸시는 실행기가 `npm test`를 다시 통과시킨 뒤에 한다.
네가 먼저 밀면 검사가 사후약방문이 되고, 깨진 문서가 main에 남는다.
(약속이 아니라 강제다 — `git push`는 [`.claude/retro-settings.json`](.claude/retro-settings.json)에서
거부된다. 막혔다고 우회하려 들지 마라.)

## 아무것도 안 할 때

교훈이 없으면 **커밋하지 말고 조용히 끝내라.** 억지로 채운 항목이 문서를 늘리는 것이
가장 큰 손해다. 요약만 job summary에 남기면 된다.
